/* Utiliza destructuring de arrays para extraer
 * el primero y el tercer elemento
 * */
var valores = [1, 2, 3, 4];

var primero = valores[0];
var tercero = valores[2];

console.log(primero, tercero); // 1 3
